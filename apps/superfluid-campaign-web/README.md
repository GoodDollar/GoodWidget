# Superfluid campaign web app

Minimal standalone Vite page for the live Superfluid Campaign Widget.

For AppKit wallet connection, set `VITE_REOWN_PROJECT_ID`. Without it, the page
uses an injected-wallet fallback.

Deploy this directory as the root of its own Vercel project.

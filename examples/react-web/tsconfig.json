{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "strict": false,
    "baseUrl": ".",
    "paths": {
      "react-native": ["react-native-web"],
      "tamagui": ["src/types/tamagui-shim.d.ts"] // This is temporary shim to avoid transpiling tamagui source. its expected to be resolved after upgrading tamagui version to a more recent one.
    }
  },
  "include": ["src"]
}

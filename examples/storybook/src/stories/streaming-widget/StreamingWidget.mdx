import { Canvas, Meta } from '@storybook/blocks';
import * as ShowcaseStories from './StreamingWidget.stories';
import { DocsCallout, DocsCard, DocsGrid, DocsPage, DocsSection } from '../docs/DocsLayout';

<Meta of={ShowcaseStories} />

<DocsPage
  eyebrow="Widget Guide"
  title="StreamingWidget"
  lead="This widget combines a product-facing injected-wallet showcase with deterministic streaming fixtures for QA, screenshots, and state-flow validation."
>
  <DocsSection
    title="Manual wallet showcase"
    description="Use this story when validating the SDK-backed streaming flow in a browser with a real EIP-1193 wallet extension. Use the Controls panel below the canvas to preview `defaultTheme` and a sample `themeOverrides` brand preset, or set a TheGraph `apiKey`."
  >
    <Canvas of={ShowcaseStories.InjectedWallet} />
  </DocsSection>

  <DocsSection
    title="QA coverage"
    description="Deterministic streaming states live in a dedicated QA story file so visual regression, fixture review, and debugging stay reproducible."
  >
    <DocsCard
      title="Open the QA fixture demo"
      href="?path=/story/qa-streamingwidget-runtime-fixtures--populated-state"
    >
      Wallet gates, network handling, loading, empty, error, stream forms, pool claims, and SUP
      reserve states live in `QA / StreamingWidget / Runtime Fixtures`. Dark theme is the default;
      `LightThemePopulated` demonstrates the explicit light-theme branch.
    </DocsCard>
  </DocsSection>

  <DocsSection
    title="Why this widget is split"
    description="The runtime-connected wallet path is useful for manual validation, while adapter-backed fixtures are better suited to automation and repeatable review."
  >
    <DocsGrid>
      <DocsCard title="Showcase stories">
        Manual review in a browser with an injected wallet and optional TheGraph API key.
      </DocsCard>
      <DocsCard title="QA stories">
        Controlled adapter states for screenshots, automation, and debugging without live runtime
        dependencies.
      </DocsCard>
    </DocsGrid>
  </DocsSection>

  <DocsSection
    title="Workflow guidance"
    description="Choose the story type based on whether you are reviewing integration behavior or validating deterministic widget states."
  >
    <DocsCallout title="Rule of use" tone="info">
      Use the showcase story for product-facing wallet checks. Use the QA fixtures when you need
      repeatable screenshots, runtime-state coverage, or debugging.
    </DocsCallout>
  </DocsSection>
</DocsPage>
